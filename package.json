{
  "name": "yarn-workspace-test",
  "packageManager": "yarn@4.1.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "todos:dev": "yarn workspace todolist dev",
    "build:all": "yarn workspaces foreach --all run build"
  }
}
